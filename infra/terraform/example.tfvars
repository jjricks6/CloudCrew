# Copy to terraform.tfvars and fill in your values.
# terraform.tfvars is gitignored — never commit personal config.

aws_region            = "us-east-1"
environment           = "dev"
budget_alert_email    = "your-email@example.com"
monthly_budget_amount = 50
