# DynamoDB tables.
# - Task ledger (project state, phase tracking, agent assignments)
# - Approval tokens (waitForTaskToken storage)
# All tables use PAY_PER_REQUEST billing (on-demand, zero cost when idle).
